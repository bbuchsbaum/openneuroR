---
phase: 10-spaces-and-s3-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/backend-s3.R
  - R/backend-dispatch.R
autonomous: true

must_haves:
  truths:
    - "S3 backend can download from openneuro-derivatives bucket (not just openneuro.org)"
    - "Bucket accessibility is probed lazily on first use (not at package load)"
    - "Verbose logging during fallback attempts (S3 failed, trying DataLad...)"
    - "Existing openneuro.org bucket downloads continue to work"
  artifacts:
    - path: "R/backend-s3.R"
      provides: "Parameterized .download_s3() with bucket argument and .probe_s3_bucket() helper"
      min_lines: 120
    - path: "R/backend-dispatch.R"
      provides: "Updated .download_with_backend() with bucket-aware dispatch and verbose logging"
      min_lines: 140
  key_links:
    - from: "R/backend-dispatch.R"
      to: "R/backend-s3.R"
      via: ".download_s3() call with bucket parameter"
      pattern: "\\.download_s3\\([^)]*bucket"
    - from: "R/backend-s3.R"
      to: "R/discovery-cache.R"
      via: ".discovery_cache for bucket probe caching"
      pattern: "\\.discovery_cache\\$"
---

<objective>
Extend S3 backend to support the openneuro-derivatives bucket for downloading pre-computed derivatives, with parameterized bucket selection and lazy accessibility probing.

Purpose: Enable Phase 11 download integration by providing infrastructure to download from s3://openneuro-derivatives/ bucket with proper fallback chain.

Output: Modified R/backend-s3.R and R/backend-dispatch.R with bucket parameterization and verbose fallback logging.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-spaces-and-s3-backend/10-CONTEXT.md
@.planning/phases/10-spaces-and-s3-backend/10-RESEARCH.md

@R/backend-s3.R
@R/backend-dispatch.R
@R/backend-detect.R
@R/discovery-cache.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize .download_s3() and add bucket probing</name>
  <files>R/backend-s3.R</files>
  <action>
Modify R/backend-s3.R:

1. Update .download_s3() signature to add bucket parameter:
   ```r
   .download_s3 <- function(dataset_id, dest_dir, files = NULL, quiet = FALSE,
                            timeout = 1800, bucket = "openneuro.org")
   ```

2. Update S3 URI construction to use parameterized bucket:
   ```r
   s3_uri <- paste0("s3://", bucket, "/", dataset_id)
   ```
   Note: For derivatives, caller will construct dataset_id as "{pipeline}/{dataset_id}-{pipeline}"

3. Update error message to include bucket info:
   ```r
   "i" = paste0("Bucket: ", bucket)
   ```

4. Add new .probe_s3_bucket() function:
   - Signature: `.probe_s3_bucket(bucket, test_path = NULL, refresh = FALSE)`
   - Uses .discovery_cache for session caching with key "s3_bucket_probe_{bucket}"
   - If test_path provided, probe that specific path (better for buckets with restricted ListObjects)
   - Otherwise probe bucket root
   - Use: aws s3 ls --no-sign-request s3://{bucket}/{test_path} --max-items 1
   - Timeout: 10 seconds
   - Return TRUE if accessible, FALSE otherwise
   - Log verbose info via cli::cli_alert_info() when probing

5. Update roxygen documentation for .download_s3():
   - Document new bucket parameter
   - Update @details to mention derivatives bucket support
   - Add example showing derivatives bucket usage
  </action>
  <verify>
R CMD check passes (no syntax errors, documentation builds)
  </verify>
  <done>
.download_s3() accepts bucket parameter with default "openneuro.org", .probe_s3_bucket() exists for lazy accessibility checking with session caching
  </done>
</task>

<task type="auto">
  <name>Task 2: Update dispatch with bucket-aware fallback and verbose logging</name>
  <files>R/backend-dispatch.R</files>
  <action>
Modify R/backend-dispatch.R:

1. Update .download_with_backend() signature:
   ```r
   .download_with_backend <- function(dataset_id, dest_dir, files = NULL,
                                      backend = NULL, quiet = FALSE,
                                      timeout = 1800, bucket = "openneuro.org")
   ```

2. Pass bucket to .download_s3():
   ```r
   "s3" = .download_s3(dataset_id, dest_dir, files, quiet, timeout, bucket)
   ```

3. Add verbose logging during fallback (per CONTEXT.md requirement):
   - Before each backend attempt (when not quiet):
     ```r
     if (!quiet) cli::cli_alert_info("Trying S3 backend for bucket {.val {bucket}}...")
     ```
   - On failure:
     ```r
     if (!quiet) cli::cli_alert_warning("S3 failed: {conditionMessage(e)}, trying {.val {fallback}}...")
     ```

4. Update error handler to preserve context:
   - Include which bucket failed
   - Include which backends were attempted

5. Consider special handling for derivatives bucket:
   - If bucket == "openneuro-derivatives" and S3 fails, DataLad clone URL differs:
     - Standard: github.com/OpenNeuroDatasets/{dataset_id}
     - Derivatives: github.com/OpenNeuroDerivatives/{dataset_id}-{pipeline}
   - This may require the caller to handle, or add a comment noting Phase 11 will address

6. Update roxygen documentation:
   - Document bucket parameter
   - Update @details for multi-bucket support
  </action>
  <verify>
R CMD check passes
Existing tests still pass (default bucket = "openneuro.org" maintains backward compatibility)
  </verify>
  <done>
.download_with_backend() passes bucket to .download_s3(), verbose logging shows "S3 failed, trying DataLad..." during fallback, bucket info included in error messages
  </done>
</task>

</tasks>

<verification>
1. R CMD check passes with 0 errors, 0 warnings
2. Existing tests pass (backward compatibility with openneuro.org bucket)
3. .download_s3() accepts bucket parameter
4. .probe_s3_bucket() works with session caching
5. Verbose fallback messages appear when not quiet
</verification>

<success_criteria>
- .download_s3() can target openneuro-derivatives bucket via bucket parameter
- Default bucket remains "openneuro.org" for backward compatibility
- .probe_s3_bucket() caches accessibility check per session
- Verbose logging shows "S3 failed, trying DataLad..." during fallback
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-spaces-and-s3-backend/10-02-SUMMARY.md`
</output>
