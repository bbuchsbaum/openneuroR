---
phase: 01-foundation-discovery
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - R/api-search.R
  - R/api-dataset.R
  - R/api-snapshots.R
  - R/api-files.R
  - R/utils-response.R
  - NAMESPACE
autonomous: true

must_haves:
  truths:
    - "User can call on_search('fmri') and get a tibble of matching datasets"
    - "User can call on_dataset('ds000001') and get metadata tibble"
    - "User can call on_snapshots('ds000001') and see tags/timestamps"
    - "User can call on_files('ds000001') and see filename/size/annexed"
  artifacts:
    - path: "R/api-search.R"
      provides: "Dataset search function"
      exports: ["on_search"]
    - path: "R/api-dataset.R"
      provides: "Single dataset metadata"
      exports: ["on_dataset"]
    - path: "R/api-snapshots.R"
      provides: "Snapshot listing"
      exports: ["on_snapshots"]
    - path: "R/api-files.R"
      provides: "File listing"
      exports: ["on_files"]
  key_links:
    - from: "R/api-search.R"
      to: "R/graphql.R"
      via: "on_request() for query execution"
      pattern: "on_request"
    - from: "R/api-search.R"
      to: "inst/graphql/search_datasets.gql"
      via: ".on_read_gql() loading"
      pattern: "\\.on_read_gql.*search"
    - from: "R/api-dataset.R"
      to: "R/graphql.R"
      via: "on_request() for query execution"
      pattern: "on_request"
    - from: "All api-*.R"
      to: "tibble"
      via: "Return tibble results"
      pattern: "tibble::tibble"
---

<objective>
Implement the four discovery API functions that fulfill Phase 1 requirements.

Purpose: Give researchers the ability to search OpenNeuro datasets, query metadata, list snapshots, and explore file contents - all returning tidy tibbles ready for analysis.

Output: Four exported functions (on_search, on_dataset, on_snapshots, on_files) that satisfy requirements DISC-01 through DISC-04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-discovery/01-RESEARCH.md
@.planning/phases/01-foundation-discovery/01-CONTEXT.md
@.planning/phases/01-foundation-discovery/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement on_search() and on_dataset()</name>
  <files>
    R/api-search.R
    R/api-dataset.R
    R/utils-response.R
    NAMESPACE
  </files>
  <action>
Implement search and single-dataset functions:

1. **R/api-search.R** - Dataset search (DISC-01):
   ```r
   #' Search OpenNeuro Datasets
   #'
   #' @param query Text query to search (NULL returns all)
   #' @param modality Filter by modality (e.g., "MRI", "EEG")
   #' @param species Filter by species
   #' @param limit Maximum results per page (default 50)
   #' @param all If TRUE, paginate through all results
   #' @param client OpenNeuro client (default: on_client())
   #' @return A tibble with columns: id, name, created, public, modalities, subjects, tasks
   #' @export
   on_search <- function(query = NULL, modality = NULL, species = NULL,
                         limit = 50, all = FALSE, client = NULL) {
     # Implementation following RESEARCH.md pattern
     # - Load query with .on_read_gql("search_datasets")
     # - Build variables list
     # - If all=FALSE, single request and return
     # - If all=TRUE, paginate using hasNextPage/endCursor
     # - Parse response with .parse_datasets()
     # - Return tibble
   }
   ```

   Key implementation details:
   - Default limit=50 (sensible page size)
   - modality/species filters passed to filterBy object in GraphQL
   - Pagination loop with repeat/break pattern from RESEARCH.md
   - Parse latestSnapshot.summary for modalities/subjects/tasks columns
   - Use list-column for summary if nested data is complex
   - Handle empty results (return zero-row tibble with correct columns)

2. **R/api-dataset.R** - Single dataset metadata (DISC-02):
   ```r
   #' Get Dataset Metadata
   #'
   #' @param id Dataset ID (e.g., "ds000001")
   #' @param client OpenNeuro client (default: on_client())
   #' @return A tibble with one row containing dataset metadata
   #' @export
   on_dataset <- function(id, client = NULL) {
     # Implementation following RESEARCH.md pattern
     # - Validate id is non-empty string
     # - Load query, execute with on_request()
     # - Check if dataset is NULL (not found)
     # - Parse and return tibble
   }
   ```

   Return columns: id, name, created, modified, public

3. **Update R/utils-response.R** - Add parsing helpers:
   - `.parse_datasets(response)` - Convert datasets query response to tibble
   - `.parse_single_dataset(dataset)` - Convert single dataset to tibble row
   - Use vapply() for type-safe extraction
   - Handle NULL values with %||% NA_type_

4. **Update NAMESPACE** - Export new functions:
   ```
   export(on_client)
   export(on_search)
   export(on_dataset)
   ```

Important from CONTEXT.md decisions:
- Return snake_case column names (not camelCase from API)
- Parse timestamps to POSIXct
- Practical column set (id, name, created, updated, public, modalities)
  </action>
  <verify>
```r
devtools::load_all('.')

# Test on_search (DISC-01)
results <- on_search("visual cortex", limit = 5)
inherits(results, "tbl_df")  # TRUE
ncol(results) >= 4           # TRUE (id, name, created, public at minimum)
nrow(results) > 0            # TRUE (should find something)

# Test empty search
empty <- on_search("xyznonexistent12345query", limit = 5)
nrow(empty) == 0             # TRUE
ncol(empty) >= 4             # TRUE (same columns)

# Test on_dataset (DISC-02)
ds <- on_dataset("ds000001")
inherits(ds, "tbl_df")       # TRUE
ds$id == "ds000001"          # TRUE
!is.na(ds$name)              # TRUE
inherits(ds$created, "POSIXct")  # TRUE
```
  </verify>
  <done>
- on_search() returns tibble of matching datasets with id, name, created, public columns
- on_search() handles empty results (zero-row tibble with correct columns)
- on_search() supports modality/species filters (at least passes params)
- on_dataset() returns single-row tibble with dataset metadata
- on_dataset() throws informative error for non-existent dataset
- All column names are snake_case
- Timestamps are POSIXct
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement on_snapshots() and on_files()</name>
  <files>
    R/api-snapshots.R
    R/api-files.R
    R/utils-response.R
    NAMESPACE
  </files>
  <action>
Implement snapshot and file listing functions:

1. **R/api-snapshots.R** - Snapshot listing (DISC-03):
   ```r
   #' List Dataset Snapshots
   #'
   #' @param id Dataset ID (e.g., "ds000001")
   #' @param client OpenNeuro client (default: on_client())
   #' @return A tibble with columns: tag, created, size
   #' @export
   on_snapshots <- function(id, client = NULL) {
     # Implementation following RESEARCH.md pattern
     # - Load query with .on_read_gql("get_snapshots")
     # - Execute with on_request()
     # - Parse response$dataset$snapshots
     # - Return tibble with tag, created (POSIXct), size (bytes)
   }
   ```

   Implementation details:
   - Return empty tibble if no snapshots (with correct column types)
   - Order by most recent first (or preserve API order)
   - size in bytes (numeric)

2. **R/api-files.R** - File listing (DISC-04):
   ```r
   #' List Files in Snapshot
   #'
   #' @param id Dataset ID (e.g., "ds000001")
   #' @param tag Snapshot tag (default: latest)
   #' @param tree Subdirectory key for nested files (default: root)
   #' @param client OpenNeuro client (default: on_client())
   #' @return A tibble with columns: filename, size, directory, annexed, key
   #' @export
   on_files <- function(id, tag = NULL, tree = NULL, client = NULL) {
     # Implementation following RESEARCH.md pattern
     # - If tag is NULL, call on_snapshots() to get latest
     # - Load query with .on_read_gql("get_files")
     # - Execute with variables: datasetId, tag, tree
     # - Parse response$snapshot$files
     # - Return tibble
   }
   ```

   Implementation details:
   - Default to latest snapshot if tag not specified
   - tree parameter allows drilling into subdirectories (use key from prior call)
   - Include key column so users can use it to explore subdirectories
   - directory column (TRUE if entry is a folder)
   - annexed column (TRUE if file is in git-annex, important for download strategy)

3. **Update R/utils-response.R** - Add parsing helpers:
   - `.parse_snapshots(response)` - Convert snapshots list to tibble
   - `.parse_files(response)` - Convert files list to tibble

4. **Update NAMESPACE** - Export new functions:
   ```
   export(on_client)
   export(on_search)
   export(on_dataset)
   export(on_snapshots)
   export(on_files)
   ```

Important:
- on_files() depends on on_snapshots() when tag not provided (internal call)
- Handle empty file lists (return zero-row tibble)
- Use rlang::abort() with class "openneuro_not_found_error" for missing datasets/snapshots
  </action>
  <verify>
```r
devtools::load_all('.')

# Test on_snapshots (DISC-03)
snaps <- on_snapshots("ds000001")
inherits(snaps, "tbl_df")    # TRUE
"tag" %in% names(snaps)      # TRUE
"created" %in% names(snaps)  # TRUE
nrow(snaps) > 0              # TRUE (ds000001 has snapshots)
inherits(snaps$created, "POSIXct")  # TRUE

# Test on_files (DISC-04) - with explicit tag
files <- on_files("ds000001", tag = snaps$tag[1])
inherits(files, "tbl_df")    # TRUE
all(c("filename", "size", "directory", "annexed") %in% names(files))  # TRUE
nrow(files) > 0              # TRUE

# Test on_files with default (latest snapshot)
files2 <- on_files("ds000001")
nrow(files2) > 0             # TRUE

# Test subdirectory access (if applicable)
subdirs <- files[files$directory, ]
if (nrow(subdirs) > 0) {
  subfiles <- on_files("ds000001", tag = snaps$tag[1], tree = subdirs$key[1])
  nrow(subfiles) >= 0  # TRUE (might be empty, but should work)
}
```
  </verify>
  <done>
- on_snapshots() returns tibble of snapshots with tag, created, size columns
- on_snapshots() handles datasets with no snapshots (empty tibble)
- on_files() returns tibble with filename, size, directory, annexed, key columns
- on_files() defaults to latest snapshot when tag not provided
- on_files() supports tree parameter for subdirectory exploration
- All functions handle errors gracefully with informative messages
  </done>
</task>

<task type="auto">
  <name>Task 3: Add roxygen documentation and verify exports</name>
  <files>
    R/api-search.R
    R/api-dataset.R
    R/api-snapshots.R
    R/api-files.R
    R/client.R
    NAMESPACE
  </files>
  <action>
Finalize documentation and exports:

1. **Ensure all exported functions have complete roxygen2 documentation:**
   - @title
   - @description
   - @param for each parameter with type and description
   - @return describing tibble structure
   - @export
   - @examples (marked \dontrun{} since they hit real API)

2. **Run roxygen2 to regenerate NAMESPACE:**
   ```r
   devtools::document()
   ```

3. **Verify NAMESPACE contains all exports:**
   - on_client
   - on_search
   - on_dataset
   - on_snapshots
   - on_files

4. **Run R CMD check to catch any issues:**
   ```bash
   R CMD check --no-manual .
   ```
   - Fix any ERROR level issues
   - Document any WARNING level issues (OK if they're expected)
  </action>
  <verify>
```bash
# Generate documentation
Rscript -e "devtools::document()"

# Check exports
grep "^export" NAMESPACE | wc -l  # Should be 5

# Run check
R CMD build . --no-build-vignettes
R CMD check --no-manual openneuro_*.tar.gz
# Should have 0 errors
```
  </verify>
  <done>
- All 5 functions (on_client, on_search, on_dataset, on_snapshots, on_files) exported
- All functions have roxygen documentation with @param, @return, @examples
- NAMESPACE generated by roxygen2
- R CMD check passes with 0 errors (warnings acceptable at this stage)
  </done>
</task>

</tasks>

<verification>
1. DISC-01 satisfied: `on_search("word")` returns tibble of datasets
2. DISC-02 satisfied: `on_dataset("ds000001")` returns metadata tibble
3. DISC-03 satisfied: `on_snapshots("ds000001")` returns tags/timestamps
4. DISC-04 satisfied: `on_files("ds000001")` returns filename/size/annexed
5. All functions exported and documented
6. R CMD check passes (0 errors)

**Integration test:**
```r
library(openneuro)

# Full workflow
results <- on_search("visual", limit = 3)
print(results)

dataset_id <- results$id[1]
metadata <- on_dataset(dataset_id)
print(metadata)

snapshots <- on_snapshots(dataset_id)
print(snapshots)

files <- on_files(dataset_id)
print(files)
```
</verification>

<success_criteria>
- [ ] on_search() works with text query, returns tibble
- [ ] on_search() handles pagination with all=TRUE
- [ ] on_dataset() returns single dataset metadata
- [ ] on_snapshots() lists all snapshots for a dataset
- [ ] on_files() lists files with size and annexed status
- [ ] Empty results return zero-row tibbles (not errors)
- [ ] All timestamps are POSIXct
- [ ] All column names are snake_case
- [ ] R CMD check: 0 errors
- [ ] Requirements DISC-01, DISC-02, DISC-03, DISC-04 marked as testable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-discovery/01-02-SUMMARY.md`
</output>
