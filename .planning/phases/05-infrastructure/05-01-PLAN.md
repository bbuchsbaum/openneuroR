---
phase: 05-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - tests/testthat.R
  - tests/testthat/helper-httptest2.R
  - tests/testthat/helper-mocks.R
  - tests/testthat/test-client.R
  - tests/testthat/test-api-search.R
  - tests/testthat/test-api-dataset.R
  - tests/testthat/test-api-files.R
autonomous: true

must_haves:
  truths:
    - "Tests exist and run without network calls"
    - "httptest2 mocking captures real API response structure"
    - "API functions (on_search, on_dataset, on_snapshots, on_files) have test coverage"
  artifacts:
    - path: "tests/testthat.R"
      provides: "testthat loader"
      contains: "test_check"
    - path: "tests/testthat/helper-httptest2.R"
      provides: "httptest2 library load"
      contains: "library(httptest2)"
    - path: "tests/testthat/helper-mocks.R"
      provides: "Common mock helpers"
      min_lines: 10
    - path: "tests/testthat/test-client.R"
      provides: "Tests for on_client"
      contains: "test_that"
    - path: "tests/testthat/test-api-search.R"
      provides: "Tests for on_search"
      contains: "test_that"
  key_links:
    - from: "tests/testthat/test-api-search.R"
      to: "httptest2::with_mock_dir"
      via: "mocking HTTP calls"
      pattern: "with_mock_dir"
---

<objective>
Set up test infrastructure with httptest2 and create mocked tests for all API discovery functions.

Purpose: Establish test framework that runs without network calls, enabling CRAN compliance (INFR-02). Cover the API surface from Phase 1 (on_client, on_search, on_dataset, on_snapshots, on_files).

Output: Working test suite with httptest2 mocking, helper files, and tests for all GraphQL-based discovery functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastructure/05-CONTEXT.md
@.planning/phases/05-infrastructure/05-RESEARCH.md

# Source files to test
@R/client.R
@R/api-search.R
@R/api-dataset.R
@R/api-snapshots.R
@R/api-files.R
@R/graphql.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up test infrastructure with httptest2</name>
  <files>
    - DESCRIPTION
    - tests/testthat.R
    - tests/testthat/helper-httptest2.R
    - tests/testthat/helper-mocks.R
  </files>
  <action>
1. Update DESCRIPTION Suggests to include httptest2:
   ```
   Suggests:
       testthat (>= 3.0.0),
       httptest2,
       withr
   ```
   Remove vcr and webmockr (not using them, httptest2 is sufficient).

2. Create tests/testthat.R (standard testthat loader):
   ```r
   library(testthat)
   library(openneuro)

   test_check("openneuro")
   ```

3. Create tests/testthat/helper-httptest2.R:
   ```r
   # Load httptest2 for all tests
   library(httptest2)
   ```

4. Create tests/testthat/helper-mocks.R with common test helpers:
   - `local_temp_cache()` - creates temp cache dir using withr::local_tempdir and sets openneuro.cache_root option
   - `skip_if_live_tests()` - skips test if OPENNEURO_LIVE_TESTS env var is NOT set (inverse of typical pattern - used for optional live tests)

   Example pattern from research:
   ```r
   local_temp_cache <- function(env = parent.frame()) {
     tmp <- withr::local_tempdir(.local_envir = env)
     withr::local_options(openneuro.cache_root = tmp, .local_envir = env)
     tmp
   }
   ```
  </action>
  <verify>
    - `devtools::load_all()` succeeds
    - `devtools::test()` runs (may report no tests yet)
    - Files exist at correct paths
  </verify>
  <done>Test infrastructure files exist, package loads, testthat runs without error</done>
</task>

<task type="auto">
  <name>Task 2: Create mocked tests for API discovery functions</name>
  <files>
    - tests/testthat/test-client.R
    - tests/testthat/test-api-search.R
    - tests/testthat/test-api-dataset.R
    - tests/testthat/test-api-files.R
    - tests/testthat/api.openneuro.org/ (auto-generated mock directory)
  </files>
  <action>
Create test files for each API module. Use httptest2::with_mock_dir() to record/replay HTTP responses.

**test-client.R:**
- Test on_client() creates openneuro_client object with correct class
- Test on_client() reads OPENNEURO_API_KEY env var when present
- Test on_client() handles missing token gracefully
- Test print.openneuro_client() produces output

**test-api-search.R:**
- Test on_search() returns tibble with expected columns (id, name, created, public, modalities, n_subjects, tasks)
- Test on_search(limit = 5) respects limit
- Use with_mock_dir() to avoid real API calls
- Use ds000001 as stable test dataset ID

**test-api-dataset.R:**
- Test on_dataset("ds000001") returns tibble with dataset metadata
- Test on_dataset() with invalid ID returns appropriate error (not_found_error class)
- Test on_snapshots("ds000001") returns tibble with tag, created columns

**test-api-files.R:**
- Test on_files("ds000001") returns tibble with filename, size, annexed columns
- Test on_files() with specific tag works

**Recording mock files:**
First run with real network to record responses. httptest2 saves to tests/testthat/api.openneuro.org/ directory. Subsequent runs replay from files.

Example test pattern:
```r
test_that("on_search returns tibble of datasets", {
  with_mock_dir("api.openneuro.org", {
    result <- on_search(limit = 5)
    expect_s3_class(result, "tbl_df")
    expect_named(result, c("id", "name", "created", "public", "modalities", "n_subjects", "tasks"))
    expect_true(nrow(result) >= 1)
  })
})
```
  </action>
  <verify>
    - `devtools::test()` runs all tests and passes
    - Mock files exist in tests/testthat/api.openneuro.org/
    - Tests pass when run offline (disable network and run again)
  </verify>
  <done>All API discovery functions have passing mocked tests, no network calls during test runs</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
cd /Users/bbuchsbaum/code/openneuroR
Rscript -e "devtools::test()"
```

Verify no network calls by running with httptest2::without_internet():
```r
httptest2::without_internet({
  testthat::test_file("tests/testthat/test-api-search.R")
})
```
</verification>

<success_criteria>
- [ ] DESCRIPTION updated with httptest2 in Suggests
- [ ] tests/testthat.R exists and is valid
- [ ] helper-httptest2.R and helper-mocks.R exist
- [ ] test-client.R has passing tests for on_client()
- [ ] test-api-search.R has passing mocked tests for on_search()
- [ ] test-api-dataset.R has passing mocked tests for on_dataset(), on_snapshots()
- [ ] test-api-files.R has passing mocked tests for on_files()
- [ ] All tests pass without network access
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure/05-01-SUMMARY.md`
</output>
