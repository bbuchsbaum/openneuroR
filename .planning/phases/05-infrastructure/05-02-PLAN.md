---
phase: 05-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - R/doctor.R
  - tests/testthat/test-backends.R
  - tests/testthat/test-handle.R
  - tests/testthat/test-cache.R
  - tests/testthat/test-doctor.R
  - man/on_doctor.Rd (generated)
autonomous: true

must_haves:
  truths:
    - "on_doctor() displays backend status with styled output"
    - "on_doctor() returns structured list invisibly for programmatic use"
    - "Backend detection functions are tested with mocking"
    - "Handle functions are tested without triggering downloads"
  artifacts:
    - path: "R/doctor.R"
      provides: "on_doctor() implementation"
      exports: ["on_doctor"]
      min_lines: 50
    - path: "tests/testthat/test-backends.R"
      provides: "Backend detection tests"
      contains: "local_mocked_bindings"
    - path: "tests/testthat/test-handle.R"
      provides: "Handle function tests"
      contains: "test_that"
    - path: "tests/testthat/test-doctor.R"
      provides: "on_doctor tests"
      contains: "openneuro_doctor"
  key_links:
    - from: "R/doctor.R"
      to: "R/backend-detect.R"
      via: ".backend_status() calls"
      pattern: "\\.backend_status"
    - from: "tests/testthat/test-backends.R"
      to: "testthat::local_mocked_bindings"
      via: "CLI mocking"
      pattern: "local_mocked_bindings"
---

<objective>
Implement on_doctor() diagnostic function and create mocked tests for backends, handles, and cache functions.

Purpose: Complete INFR-03 (on_doctor reports backend status) and expand test coverage to non-HTTP functions using local_mocked_bindings() for CLI mocking.

Output: on_doctor() function with styled CLI output, plus comprehensive tests for backend dispatch, handle pattern, and cache management.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastructure/05-CONTEXT.md
@.planning/phases/05-infrastructure/05-RESEARCH.md
@.planning/phases/05-infrastructure/05-01-SUMMARY.md

# Source files being tested/extended
@R/backend-detect.R
@R/backend-dispatch.R
@R/backend-s3.R
@R/backend-datalad.R
@R/handle.R
@R/cache-management.R
@R/cache-path.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement on_doctor() diagnostic function</name>
  <files>
    - R/doctor.R
  </files>
  <action>
Create R/doctor.R implementing on_doctor() following the sitrep pattern from research:

```r
#' OpenNeuro Backend Diagnostics
#'
#' Reports the status of all available download backends, showing which
#' are installed, their versions, and readiness for use.
#'
#' @return Invisibly returns an object of class `openneuro_doctor` containing:
#'   \describe{
#'     \item{https}{List with available (always TRUE), version (NA)}
#'     \item{s3}{List with available (logical), version (character or NA)}
#'     \item{datalad}{List with available (logical), version (character or NA)}
#'   }
#'
#' @export
#' @examples
#' on_doctor()
on_doctor <- function() {
  # ... implementation
}
```

**Implementation details:**

1. Gather status for each backend using .backend_status(refresh = TRUE):
   - https: always available, version = NA
   - s3: check .backend_status("s3"), get version via .get_aws_version()
   - datalad: check .backend_status("datalad"), get version via .get_datalad_version()

2. Create internal version-getting helpers:
   - `.get_aws_version()` - runs `aws --version`, parses first line, returns NA on failure
   - `.get_datalad_version()` - runs `datalad --version`, parses output, returns NA on failure
   Use processx::run() with timeout = 5 seconds, error = FALSE to safely capture output.

3. Return structure:
   ```r
   structure(
     list(
       https = list(available = TRUE, version = NA_character_),
       s3 = list(available = TRUE/FALSE, version = "2.x.x" or NA),
       datalad = list(available = TRUE/FALSE, version = "0.x.x" or NA)
     ),
     class = "openneuro_doctor"
   )
   ```

4. Implement print.openneuro_doctor() with cli styling:
   ```
   -- OpenNeuro Backend Status --------------------------------

   Required:
     v HTTPS         (always available)

   Optional:
     v AWS CLI       2.15.0
     x DataLad       not installed
       i Install: pip install datalad
   ```

Use cli::cli_h1(), cli::cli_text(), cli::cli_bullets() for formatting.
Use cli::col_green() for checkmarks, cli::col_red() for X marks.

5. Add installation hints when backends missing:
   - S3 missing: "Install: https://aws.amazon.com/cli/"
   - DataLad missing: "Install: pip install datalad"
  </action>
  <verify>
    - `devtools::document()` generates man/on_doctor.Rd
    - `devtools::load_all(); on_doctor()` prints styled output
    - `result <- on_doctor(); result$s3$available` returns logical
  </verify>
  <done>on_doctor() displays styled backend status and returns structured list invisibly</done>
</task>

<task type="auto">
  <name>Task 2: Create mocked tests for backends, handles, and cache</name>
  <files>
    - tests/testthat/test-backends.R
    - tests/testthat/test-handle.R
    - tests/testthat/test-cache.R
    - tests/testthat/test-doctor.R
  </files>
  <action>
Create test files using local_mocked_bindings() for CLI tool mocking.

**test-backends.R:**
Test backend detection by mocking Sys.which and .find_aws_cli:
```r
test_that(".backend_available detects missing AWS CLI", {
  local_mocked_bindings(
    .find_aws_cli = function() ""
  )
  # Force refresh to bypass cache
  expect_false(.backend_available("s3"))
})

test_that(".backend_available detects present DataLad", {
  local_mocked_bindings(
    Sys.which = function(names) {
      result <- setNames(rep("", length(names)), names)
      if ("datalad" %in% names) result["datalad"] <- "/usr/bin/datalad"
      if ("git-annex" %in% names) result["git-annex"] <- "/usr/bin/git-annex"
      result
    }
  )
  expect_true(.backend_available("datalad"))
})

test_that(".select_backend returns https when nothing else available", {
  local_mocked_bindings(
    .backend_status = function(backend, refresh = FALSE) backend == "https"
  )
  expect_equal(.select_backend(), "https")
})

test_that(".select_backend respects priority when all available", {
  local_mocked_bindings(
    .backend_status = function(backend, refresh = FALSE) TRUE
  )
  expect_equal(.select_backend(), "datalad")  # Highest priority
})
```

**test-handle.R:**
Test handle creation and state without triggering downloads:
```r
test_that("on_handle creates pending handle", {
  handle <- on_handle("ds000001")
  expect_s3_class(handle, "openneuro_handle")
  expect_equal(handle$state, "pending")
  expect_null(handle$path)
})

test_that("on_handle validates dataset_id", {
  expect_error(on_handle(""), class = "openneuro_validation_error")
  expect_error(on_handle(123), class = "openneuro_validation_error")
})

test_that("on_handle validates backend option", {
  expect_error(
    on_handle("ds000001", backend = "invalid"),
    class = "openneuro_validation_error"
  )
})

test_that("on_path errors on unfetched handle", {
  handle <- on_handle("ds000001")
  expect_error(on_path(handle), class = "openneuro_handle_error")
})

test_that("print.openneuro_handle produces output", {
  handle <- on_handle("ds000001", files = "participants.tsv")
  expect_output(print(handle), "openneuro_handle")
  expect_output(print(handle), "pending")
})
```

**test-cache.R:**
Test cache functions using local_temp_cache() helper:
```r
test_that("on_cache_list returns empty tibble for fresh cache", {
  cache_dir <- local_temp_cache()
  result <- on_cache_list()
  expect_s3_class(result, "tbl_df")
  expect_equal(nrow(result), 0)
})

test_that(".on_cache_root respects option", {
  tmp <- withr::local_tempdir()
  withr::local_options(openneuro.cache_root = tmp)
  expect_equal(.on_cache_root(), tmp)
})
```

**test-doctor.R:**
Test on_doctor() with mocked backend status:
```r
test_that("on_doctor returns openneuro_doctor class", {
  local_mocked_bindings(
    .backend_status = function(backend, refresh = FALSE) backend == "https"
  )
  result <- on_doctor()
  expect_s3_class(result, "openneuro_doctor")
})

test_that("on_doctor contains all backends", {
  local_mocked_bindings(
    .backend_status = function(backend, refresh = FALSE) TRUE,
    .get_aws_version = function() "2.15.0",
    .get_datalad_version = function() "0.19.3"
  )
  result <- on_doctor()
  expect_named(result, c("https", "s3", "datalad"))
  expect_true(result$https$available)
  expect_true(result$s3$available)
  expect_true(result$datalad$available)
})

test_that("print.openneuro_doctor produces styled output", {
  local_mocked_bindings(
    .backend_status = function(backend, refresh = FALSE) TRUE
  )
  result <- on_doctor()
  expect_output(print(result), "OpenNeuro")
})
```
  </action>
  <verify>
    - `devtools::test()` runs all new test files and passes
    - No actual CLI tools are invoked during tests
    - Test coverage includes backend detection, handle lifecycle, cache paths
  </verify>
  <done>All backend, handle, cache, and doctor functions have passing mocked tests</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
cd /Users/bbuchsbaum/code/openneuroR
Rscript -e "devtools::test()"
```

Check on_doctor() manually:
```r
devtools::load_all()
on_doctor()
```
</verification>

<success_criteria>
- [ ] R/doctor.R implements on_doctor() with print method
- [ ] on_doctor() returns openneuro_doctor S3 class
- [ ] on_doctor() displays styled CLI output with backend status
- [ ] test-backends.R has passing tests for backend detection
- [ ] test-handle.R has passing tests for handle lifecycle
- [ ] test-cache.R has passing tests for cache functions
- [ ] test-doctor.R has passing tests for on_doctor()
- [ ] All tests use mocking (no real CLI tool invocations)
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure/05-02-SUMMARY.md`
</output>
