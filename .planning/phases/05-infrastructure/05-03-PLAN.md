---
phase: 05-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - DESCRIPTION
  - NAMESPACE
  - R/*.R (documentation fixes)
  - man/*.Rd (regenerated)
autonomous: true

must_haves:
  truths:
    - "R CMD check passes with no errors"
    - "R CMD check passes with no warnings"
    - "All exported functions have complete documentation"
    - "Examples use dontrun or work without network"
  artifacts:
    - path: "DESCRIPTION"
      provides: "Package metadata"
      contains: "openneuro"
    - path: "NAMESPACE"
      provides: "Export declarations"
      contains: "export(on_doctor)"
  key_links:
    - from: "R/*.R"
      to: "man/*.Rd"
      via: "roxygen2 documentation"
      pattern: "@export"
---

<objective>
Ensure package passes R CMD check with no errors or warnings, completing CRAN compliance.

Purpose: Complete INFR-01 (R CMD check passes). Fix any documentation issues, example problems, or NAMESPACE gaps discovered during check.

Output: Clean R CMD check run with 0 errors, 0 warnings, and minimal notes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastructure/05-CONTEXT.md
@.planning/phases/05-infrastructure/05-01-SUMMARY.md
@.planning/phases/05-infrastructure/05-02-SUMMARY.md

@DESCRIPTION
@NAMESPACE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run R CMD check and fix all errors/warnings</name>
  <files>
    - DESCRIPTION
    - NAMESPACE
    - R/*.R (as needed for fixes)
    - man/*.Rd (regenerated)
  </files>
  <action>
Run R CMD check and iteratively fix any issues found.

**Step 1: Run initial check**
```r
devtools::check()
```

**Step 2: Common issues to expect and fix:**

1. **Missing documentation** - Add @return, @param for any undocumented arguments
2. **Examples hitting network** - Wrap in \dontrun{} or add if(interactive()) guard
3. **NAMESPACE issues** - Run devtools::document() to regenerate
4. **Undeclared imports** - Add to DESCRIPTION Imports
5. **Global variable notes** - Add utils::globalVariables() or use .data$ prefix for dplyr

**Step 3: Documentation checklist**
Ensure all exported functions have:
- @title (first line of roxygen block)
- @description or first paragraph
- @param for each parameter
- @return describing return value
- @export
- @examples (with \dontrun{} for network-requiring examples)

**Step 4: Example fixes**
All examples that make network calls should use:
```r
#' @examples
#' \dontrun{
#' on_search("visual")
#' }
```

For on_client() which doesn't need network:
```r
#' @examples
#' client <- on_client()
#' print(client)
```

**Step 5: Fix any global variable NOTEs**
If dplyr causes "no visible binding" notes, add to R/openneuro-package.R or relevant file:
```r
# Silence R CMD check notes for dplyr column references
utils::globalVariables(c("id", "name", "created", ...))
```

**Step 6: Re-run check until clean**
```r
devtools::check()
```
Target: 0 errors, 0 warnings. Notes are acceptable but minimize.
  </action>
  <verify>
    - `devtools::check()` returns 0 errors, 0 warnings
    - `devtools::document()` runs without error
    - All man/*.Rd files are complete and valid
  </verify>
  <done>R CMD check passes with 0 errors, 0 warnings</done>
</task>

<task type="auto">
  <name>Task 2: Final verification and cleanup</name>
  <files>
    - DESCRIPTION (version bump if needed)
    - NEWS.md (optional - create if doesn't exist)
  </files>
  <action>
Perform final verification steps:

**Step 1: Run check with CRAN-like settings**
```r
devtools::check(cran = TRUE)
```
This applies stricter CRAN checks.

**Step 2: Verify test suite runs clean**
```r
devtools::test()
```
All tests should pass.

**Step 3: Verify package can be installed**
```r
devtools::install()
library(openneuro)
on_doctor()
```

**Step 4: Create NEWS.md if it doesn't exist**
```markdown
# openneuro 0.1.0

* Initial release
* Search and explore OpenNeuro datasets via GraphQL API
* Download datasets with multi-backend support (HTTPS, S3, DataLad)
* CRAN-compliant caching with manifest tracking
* Lazy handle pattern for pipeline integration
* Comprehensive mocked test suite
```

**Step 5: Verify all exports work**
```r
library(openneuro)
# These should all exist and have help pages:
?on_client
?on_search
?on_dataset
?on_snapshots
?on_files
?on_download
?on_handle
?on_fetch
?on_path
?on_cache_list
?on_cache_info
?on_cache_clear
?on_doctor
```
  </action>
  <verify>
    - `devtools::check(cran = TRUE)` passes
    - `devtools::test()` all pass
    - Package installs and loads without error
    - All exported functions have help pages accessible via ?
  </verify>
  <done>Package is CRAN-ready with complete documentation and passing checks</done>
</task>

</tasks>

<verification>
Final verification commands:
```bash
cd /Users/bbuchsbaum/code/openneuroR

# Full CRAN-like check
Rscript -e "devtools::check(cran = TRUE)"

# Test suite
Rscript -e "devtools::test()"

# Verify installation
Rscript -e "devtools::install(); library(openneuro); on_doctor()"
```
</verification>

<success_criteria>
- [ ] R CMD check returns 0 errors
- [ ] R CMD check returns 0 warnings
- [ ] All exported functions documented with roxygen2
- [ ] All examples either work offline or use \dontrun{}
- [ ] NAMESPACE includes all exports (including on_doctor)
- [ ] Package installs successfully
- [ ] on_doctor() runs and displays backend status
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure/05-03-SUMMARY.md`
</output>
