---
phase: 07-subject-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/subject-filter.R
  - R/download.R
  - tests/testthat/test-subject-filter.R
  - tests/testthat/test-download.R
  - NAMESPACE
  - man/regex.Rd
autonomous: true

must_haves:
  truths:
    - "User can download specific subjects via on_download(..., subjects = c('sub-01', 'sub-02'))"
    - "User can use regex patterns via on_download(..., subjects = regex('sub-0[1-5]'))"
    - "Download only retrieves files for matching subjects plus root files"
    - "Invalid subject IDs produce helpful error messages showing available subjects"
    - "Regex matching zero subjects produces error with available subjects"
  artifacts:
    - path: "R/subject-filter.R"
      provides: "regex() helper, normalization, file filtering logic"
      exports: ["regex"]
    - path: "R/download.R"
      provides: "on_download() with subjects= parameter"
      contains: "subjects = NULL"
    - path: "tests/testthat/test-subject-filter.R"
      provides: "Tests for subject filtering infrastructure"
      min_lines: 50
    - path: "tests/testthat/test-download.R"
      provides: "Tests for on_download() subjects= parameter"
      contains: "subjects ="
  key_links:
    - from: "R/download.R"
      to: "R/subject-filter.R"
      via: "is_regex(), .normalize_subject_ids(), .filter_files_by_subjects()"
      pattern: "\\.filter_files_by_subjects\\("
    - from: "R/download.R"
      to: "R/api-subjects.R"
      via: "on_subjects() for validation"
      pattern: "on_subjects\\("
---

<objective>
Add subjects= parameter to on_download() enabling selective subject downloads

Purpose: Users need to download specific subjects from large datasets without fetching everything. This saves bandwidth, storage, and time for partial analyses.

Output: Modified on_download() accepting subjects= parameter with explicit regex() wrapper for pattern matching
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-subject-filtering/07-CONTEXT.md
@.planning/phases/07-subject-filtering/07-RESEARCH.md
@.planning/phases/06-subject-querying/06-01-SUMMARY.md

@R/download.R
@R/api-subjects.R
@R/utils-response.R
@R/download-list.R
@tests/testthat/test-download.R
@tests/testthat/test-api-subjects.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subject filtering infrastructure</name>
  <files>R/subject-filter.R, tests/testthat/test-subject-filter.R, NAMESPACE</files>
  <action>
Create R/subject-filter.R with subject filtering utilities:

1. **regex() exported helper** - marks string as regex pattern:
   - Returns character vector with S3 class `c("on_regex", "character")`
   - Validates input is single non-empty character string
   - Use rlang::abort() with class "openneuro_validation_error" on invalid input
   - Full roxygen documentation with @export

2. **is_regex() internal helper** - checks for on_regex class:
   - `inherits(x, "on_regex")` pattern
   - @keywords internal

3. **.normalize_subject_id() internal** - normalizes single subject ID:
   - If already starts with "sub-", return as-is
   - Otherwise, prepend "sub-" prefix
   - Handles both "01" and "sub-01" input formats

4. **.normalize_subject_ids() internal** - vectorized wrapper:
   - Uses vapply() for type safety
   - Returns character vector of normalized IDs

5. **.validate_subjects() internal** - validates subject IDs against available:
   - Takes requested IDs, available IDs, and dataset_id
   - Normalizes both sets for comparison
   - If any invalid, abort with helpful message showing:
     - Which IDs are invalid
     - First 10 available subjects (with "..." if more)
   - Returns normalized requested IDs on success

6. **.match_subjects_regex() internal** - matches subjects against regex:
   - Takes subject_ids vector and pattern string
   - Auto-anchor pattern with ^ and $ for full match
   - Use grepl() with anchored pattern
   - Return logical vector of matches

7. **.filter_files_by_subjects() internal** - filters file tibble:
   - Takes files_df (from .list_all_files()), matching_subjects vector, include_derivatives boolean
   - Always include root-level files (no "/" in path OR matches root file patterns like dataset_description.json, README, CHANGES, participants.*, .bidsignore)
   - Include files where path starts with "sub-XX/" for any matching subject
   - If include_derivatives=TRUE, also include files matching "derivatives/*/sub-XX/" pattern
   - Return filtered tibble

Create tests/testthat/test-subject-filter.R:
- Test regex() returns correct class
- Test regex() rejects invalid inputs (empty, NULL, vector)
- Test is_regex() returns TRUE for regex(), FALSE for plain strings
- Test .normalize_subject_id() handles both formats
- Test .normalize_subject_ids() vectorized behavior
- Test .validate_subjects() accepts valid IDs
- Test .validate_subjects() errors on invalid IDs with helpful message
- Test .match_subjects_regex() with simple pattern
- Test .match_subjects_regex() anchoring (sub-01 shouldn't match sub-010)
- Test .filter_files_by_subjects() includes root files
- Test .filter_files_by_subjects() filters subject directories
- Test .filter_files_by_subjects() includes derivatives when TRUE
- Test .filter_files_by_subjects() excludes derivatives when FALSE

Update NAMESPACE (manually or via devtools::document()):
- Add export(regex)
  </action>
  <verify>
    R CMD check passes with no errors or warnings
    All tests in test-subject-filter.R pass
    regex() appears in NAMESPACE exports
  </verify>
  <done>
    Subject filtering utilities exist and tested
    regex() is exported
    Internal helpers validate and filter correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate subjects= into on_download()</name>
  <files>R/download.R, tests/testthat/test-download.R, man/on_download.Rd</files>
  <action>
Modify on_download() in R/download.R:

1. **Add subjects= parameter** to function signature:
   ```r
   on_download <- function(id, tag = NULL, files = NULL, subjects = NULL,
                           include_derivatives = TRUE, dest_dir = NULL, ...)
   ```
   Position: after files, before dest_dir

2. **Add include_derivatives= parameter**:
   - Default TRUE
   - Controls whether derivatives/*/sub-XX/ paths are included

3. **Update roxygen documentation**:
   - @param subjects Character vector of subject IDs (e.g., c("sub-01", "sub-02"))
     or a regex pattern wrapped in regex() (e.g., regex("sub-0[1-5]")).
     Subject IDs can be specified with or without the "sub-" prefix.
     If NULL (default), downloads all subjects.
   - @param include_derivatives If TRUE (default) and subjects is specified,
     also include derivative outputs for matching subjects from derivatives/ directory.

4. **Add subject filtering logic** after line ~106 (after .list_all_files() call):
   ```r
   # Filter by subjects if specified
   if (!is.null(subjects)) {
     # Get available subjects from API
     available <- on_subjects(id, tag = tag, client = client)
     available_ids <- available$subject_id

     if (is_regex(subjects)) {
       # Regex matching with auto-anchoring
       pattern <- as.character(subjects)
       matches <- .match_subjects_regex(available_ids, pattern)
       matching <- available_ids[matches]

       if (length(matching) == 0) {
         available_display <- if (length(available_ids) <= 10) {
           paste(available_ids, collapse = ", ")
         } else {
           paste(c(head(available_ids, 10), "..."), collapse = ", ")
         }
         rlang::abort(
           c("No subjects match pattern",
             "x" = paste0("Pattern '", pattern, "' matched 0 subjects"),
             "i" = paste0("Available in ", id, ": ", available_display)),
           class = "openneuro_validation_error"
         )
       }
     } else {
       # Literal IDs - validate and normalize
       matching <- .validate_subjects(subjects, available_ids, id)
     }

     # Normalize to sub- prefixed format for file path matching
     matching_normalized <- .normalize_subject_ids(matching)

     # Filter files to matching subjects + root files
     all_files <- .filter_files_by_subjects(all_files, matching_normalized, include_derivatives)

     if (nrow(all_files) == 0) {
       if (!quiet) {
         cli::cli_alert_warning(
           "No files found for subjects {.val {matching}} in dataset {.val {id}}"
         )
       }
       return(invisible(list(
         downloaded = 0L,
         skipped = 0L,
         failed = character(),
         total_bytes = 0,
         dest_dir = dest_dir %||% default_dest
       )))
     }
   }
   ```

5. **Update @examples** section:
   Add examples:
   ```r
   # Download specific subjects
   on_download("ds000001", subjects = c("sub-01", "sub-02"))

   # Download subjects matching pattern
   on_download("ds000001", subjects = regex("sub-0[1-5]"))

   # Download subjects without derivatives
   on_download("ds000001", subjects = c("01", "02"), include_derivatives = FALSE)
   ```

Add tests to tests/testthat/test-download.R:

1. **Test subjects= with literal IDs**:
   - Mock on_subjects() to return known subjects
   - Mock .list_all_files() to return files for multiple subjects
   - Call on_download() with subjects = c("sub-01")
   - Verify filtered files only include sub-01 paths + root files

2. **Test subjects= with regex()**:
   - Mock on_subjects() and .list_all_files()
   - Call on_download() with subjects = regex("sub-0[12]")
   - Verify both sub-01 and sub-02 files included

3. **Test invalid subject ID produces error**:
   - Mock on_subjects() to return c("01", "02")
   - Call on_download() with subjects = "99"
   - Expect error with class "openneuro_validation_error"

4. **Test regex matching zero subjects produces error**:
   - Mock on_subjects() to return c("01", "02")
   - Call on_download() with subjects = regex("sub-99.*")
   - Expect error with class "openneuro_validation_error"

5. **Test include_derivatives=FALSE excludes derivatives**:
   - Mock files including derivatives/fmriprep/sub-01/...
   - Call with include_derivatives = FALSE
   - Verify derivatives paths not in filtered list

6. **Test root files always included**:
   - Verify dataset_description.json, README, participants.tsv included regardless of subject filter

Run devtools::document() to update man/on_download.Rd
  </action>
  <verify>
    R CMD check passes with no errors or warnings
    All tests in test-download.R pass
    on_download() has subjects= and include_derivatives= parameters
    man/on_download.Rd updated with new parameters
  </verify>
  <done>
    on_download() accepts subjects= parameter (character vector or regex())
    on_download() accepts include_derivatives= parameter
    Invalid/empty subject matches produce helpful errors
    Root files always included with any subject filter
    All existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Final verification and documentation</name>
  <files>None (verification only)</files>
  <action>
Run comprehensive verification:

1. **R CMD check**:
   ```bash
   R CMD check --as-cran .
   ```
   Expected: 0 errors, 0 warnings, 0 notes

2. **Test suite**:
   ```bash
   Rscript -e "devtools::test()"
   ```
   Expected: All tests pass, 0 failures

3. **Verify exports**:
   ```bash
   Rscript -e "library(openneuro); exists('regex')"
   ```
   Expected: TRUE

4. **Verify help pages**:
   ```bash
   Rscript -e "library(openneuro); ?on_download; ?regex"
   ```
   Expected: Both help pages render without error

5. **Manual smoke test** (optional, can use mocks):
   - Verify on_download() signature includes subjects= and include_derivatives=
   - Verify regex() creates correct class
   - Verify is_regex() works

If any issues found, fix them before completing.
  </action>
  <verify>
    R CMD check: 0 errors, 0 warnings, 0 notes
    Test suite: All tests pass
    regex() exported and documented
    on_download() documented with new parameters
  </verify>
  <done>
    Phase 7 requirements FILT-01, FILT-02, FILT-03 complete
    Package passes R CMD check
    All tests pass
    Subject filtering fully functional
  </done>
</task>

</tasks>

<verification>
Phase verification checklist:

1. **FILT-01: subjects= parameter exists**
   - on_download() signature includes subjects = NULL
   - Parameter documented in roxygen

2. **FILT-02: character vector support**
   - c("sub-01", "sub-02") works
   - c("01", "02") also works (normalized internally)
   - Invalid IDs produce helpful error

3. **FILT-03: regex pattern support**
   - regex("sub-0[1-5]") works
   - Auto-anchored for full match
   - Empty match produces error (not warning)

4. **Root files included**
   - dataset_description.json, README, etc. always downloaded

5. **Derivatives handled**
   - include_derivatives=TRUE includes derivatives/*/sub-XX/
   - include_derivatives=FALSE excludes them

6. **Package quality**
   - R CMD check: 0 errors, 0 warnings, 0 notes
   - All tests pass
</verification>

<success_criteria>
- on_download("ds000001", subjects = c("sub-01", "sub-02")) downloads only those subjects
- on_download("ds000001", subjects = regex("sub-0[1-5]")) downloads matching subjects
- Invalid subject IDs error with helpful message showing available subjects
- Regex matching zero subjects errors (not warns)
- Root files (dataset_description.json, README, etc.) always included
- Derivatives filtered when include_derivatives=FALSE
- R CMD check passes cleanly
- Test count increases (target: ~12-15 new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/07-subject-filtering/07-01-SUMMARY.md`
</output>
