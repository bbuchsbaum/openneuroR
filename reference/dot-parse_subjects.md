# Parse Subjects Response

Converts the subjects query response to a tibble.

## Usage

``` r
.parse_subjects(response, dataset_id)
```

## Arguments

- response:

  The parsed response from on_request().

- dataset_id:

  The dataset identifier to include in output.

## Value

A tibble with subject information.
