# Format Bytes as Human-Readable String

Converts bytes to human-readable format (B, KB, MB, GB).

## Usage

``` r
.format_bytes(bytes)
```

## Arguments

- bytes:

  Number of bytes.

## Value

A character string with formatted size.
