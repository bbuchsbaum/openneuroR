# Transform Numeric Timestamps to POSIXct

Converts numeric timestamp columns to POSIXct class.

## Usage

``` r
.transform_timestamps_posix(df)
```

## Arguments

- df:

  A tibble with numeric timestamp column.

## Value

The tibble with POSIXct timestamp columns.
