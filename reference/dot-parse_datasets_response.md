# Parse Datasets List Response

Converts the datasets() query response to a tibble.

## Usage

``` r
.parse_datasets_response(response)
```

## Arguments

- response:

  The parsed response from on_request().

## Value

A tibble with dataset information.
