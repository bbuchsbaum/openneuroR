# Clear Discovery Cache

Clears all cached discovery data from the current session. Useful for
testing or when you want to force a refresh of GitHub API data.

## Usage

``` r
.discovery_cache_clear()
```

## Value

Invisibly returns `TRUE`.
