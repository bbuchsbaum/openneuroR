# Check if Object is a Regex Pattern

Tests whether an object was created by
[`regex()`](https://bbuchsbaum.github.io/openneuroR/reference/regex.md).

## Usage

``` r
is_regex(x)
```

## Arguments

- x:

  Object to test.

## Value

`TRUE` if `x` inherits from `"on_regex"`, `FALSE` otherwise.
