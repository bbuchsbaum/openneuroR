# Filter Derivative Files by Subject Regex

Filters derivative files using a regex pattern for subject matching.

## Usage

``` r
.filter_derivative_files_by_subjects_regex(files_df, pattern)
```

## Arguments

- files_df:

  A tibble with `full_path` column.

- pattern:

  Regex pattern string for subject matching.

## Value

Filtered tibble.
