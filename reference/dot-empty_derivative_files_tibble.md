# Create Empty Derivative Files Tibble

Returns an empty tibble with the structure used for derivative file
listings.

## Usage

``` r
.empty_derivative_files_tibble()
```

## Value

An empty tibble with filename, full_path, and size columns.
