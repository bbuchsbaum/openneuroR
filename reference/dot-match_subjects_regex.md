# Match Subjects Against Regex Pattern

Matches subject IDs against a regex pattern with auto-anchoring.

## Usage

``` r
.match_subjects_regex(subject_ids, pattern)
```

## Arguments

- subject_ids:

  Character vector of subject IDs to match.

- pattern:

  Regex pattern string (without anchors).

## Value

Logical vector indicating which subjects match.
