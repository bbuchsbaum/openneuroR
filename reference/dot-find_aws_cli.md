# Find AWS CLI Executable

Searches for the AWS CLI in PATH and common installation locations.

## Usage

``` r
.find_aws_cli()
```

## Value

Character string: Path to AWS CLI, or empty string if not found.
