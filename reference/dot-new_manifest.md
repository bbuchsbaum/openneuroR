# Create New Manifest

Creates an empty manifest structure for a dataset.

## Usage

``` r
.new_manifest(dataset_id, snapshot_tag)
```

## Arguments

- dataset_id:

  Dataset identifier (e.g., "ds000001").

- snapshot_tag:

  Snapshot/version tag (e.g., "1.0.0").

## Value

A new manifest list structure.
