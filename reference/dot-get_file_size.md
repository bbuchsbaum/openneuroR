# Get File Size Safely

Returns the size of a file in bytes, or 0 if the file doesn't exist.

## Usage

``` r
.get_file_size(path)
```

## Arguments

- path:

  Path to the file.

## Value

Numeric file size in bytes, or 0 if file doesn't exist.
