# Get DataLad Version

Runs `datalad --version` and parses the output to extract version
string.

## Usage

``` r
.get_datalad_version()
```

## Value

Character string with version, or NA_character\_ on failure.
