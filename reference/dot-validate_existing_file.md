# Validate Existing File

Checks if a file exists and has the expected size.

## Usage

``` r
.validate_existing_file(path, expected_size)
```

## Arguments

- path:

  Path to the file to check.

- expected_size:

  Expected file size in bytes.

## Value

`TRUE` if file exists with correct size, `FALSE` otherwise.
