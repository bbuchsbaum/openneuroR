# Check if String Contains Regex Metacharacters

Detects if a string appears to be a regex pattern by checking for common
metacharacters.

## Usage

``` r
.is_regex(x)
```

## Arguments

- x:

  Character string to check.

## Value

`TRUE` if `x` appears to be a regex pattern, `FALSE` otherwise.
