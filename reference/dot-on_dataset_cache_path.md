# Get Dataset Cache Path

Returns the cache directory path for a specific dataset. Does NOT
auto-create the directory.

## Usage

``` r
.on_dataset_cache_path(dataset_id)
```

## Arguments

- dataset_id:

  Dataset identifier (e.g., "ds000001").

## Value

Path to the dataset cache directory.
