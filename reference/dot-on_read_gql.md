# Read GraphQL Query from File

Internal function to load GraphQL queries from inst/graphql/ directory.

## Usage

``` r
.on_read_gql(name)
```

## Arguments

- name:

  The query name (without .gql extension).

## Value

The query string.
