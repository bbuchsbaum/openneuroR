# Parse Single Dataset Response

Converts a single dataset to a tibble row.

## Usage

``` r
.parse_single_dataset(dataset)
```

## Arguments

- dataset:

  The dataset object from response.

## Value

A tibble with one row.
