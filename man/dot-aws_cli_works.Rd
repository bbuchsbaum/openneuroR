% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-detect.R
\name{.aws_cli_works}
\alias{.aws_cli_works}
\title{Check that the AWS CLI Actually Runs}
\usage{
.aws_cli_works(aws_path)
}
\arguments{
\item{aws_path}{Path to the AWS CLI executable.}
}
\value{
Logical: TRUE if \code{aws --version} exits with status 0, else FALSE.
}
\description{
A binary named \code{aws} on the PATH is not sufficient: broken installations
(for example a Python entry point whose \code{awscli} module is missing) are
present but non-functional. This invokes \code{aws --version} and reports
success only when the command exits cleanly, so backend auto-selection
never picks an S3 backend that cannot run.
}
\keyword{internal}
