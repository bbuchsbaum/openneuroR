% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handle.R
\name{on_fetch}
\alias{on_fetch}
\alias{on_fetch.openneuro_handle}
\title{Fetch Handle (Materialize Download)}
\usage{
on_fetch(handle, ...)

\method{on_fetch}{openneuro_handle}(handle, quiet = FALSE, force = FALSE, ...)
}
\arguments{
\item{handle}{An object to fetch. For \code{openneuro_handle} objects,
triggers the download.}

\item{...}{Additional arguments passed to methods.}

\item{quiet}{If TRUE, suppress progress output during download.}

\item{force}{If TRUE, re-download even if handle is already "ready".}
}
\value{
The handle with updated state. For \code{openneuro_handle},
returns the handle with \code{state = "ready"}, \code{path} set to the
download location, and \code{fetch_time} set to current time.
}
\description{
Materializes a lazy handle by downloading the referenced dataset.
If the handle is already in "ready" state, returns it unchanged
unless \code{force = TRUE}.
}
\section{Important}{

You must capture the return value! S3 objects have copy semantics:
\preformatted{
# CORRECT
handle <- on_fetch(handle)

# WRONG - changes are lost
on_fetch(handle)
}
}

\examples{
\dontrun{
handle <- on_handle("ds000001", files = "participants.tsv")
handle <- on_fetch(handle)  # Downloads now
handle$state  # "ready"
}
}
\seealso{
\code{\link[=on_handle]{on_handle()}} to create a handle, \code{\link[=on_path]{on_path()}} to get path.
}
