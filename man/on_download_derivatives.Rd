% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download-derivatives.R
\name{on_download_derivatives}
\alias{on_download_derivatives}
\title{Download Derivative Dataset}
\usage{
on_download_derivatives(
  dataset_id,
  pipeline,
  subjects = NULL,
  space = NULL,
  suffix = NULL,
  dry_run = FALSE,
  dest_dir = NULL,
  use_cache = TRUE,
  quiet = FALSE,
  verbose = FALSE,
  force = FALSE,
  backend = NULL,
  client = NULL
)
}
\arguments{
\item{dataset_id}{Dataset identifier (e.g., "ds000001").}

\item{pipeline}{Pipeline name (e.g., "fmriprep", "mriqc").}

\item{subjects}{Character vector of subject IDs (e.g., \code{c("sub-01", "sub-02")})
or a regex pattern wrapped in \code{\link[=regex]{regex()}} (e.g., \code{regex("sub-0[1-5]")}).
Subject IDs can be specified with or without the "sub-" prefix.
If \code{NULL} (default), downloads all subjects.}

\item{space}{Character string: output space to filter by (e.g.,
"MNI152NLin2009cAsym", "fsaverage", "T1w"). If \code{NULL} (default),
downloads all spaces. Matching is exact (specify full space name).
Files without a \verb{_space-} entity (native space) are always included.}

\item{suffix}{Character vector of BIDS suffixes to filter by (e.g.,
\code{c("bold", "T1w", "mask")}). If \code{NULL} (default), downloads all suffixes.
Files without a clear suffix (metadata files) are always included.}

\item{dry_run}{If \code{TRUE}, returns a tibble of files that would be downloaded
without actually downloading them. Default is \code{FALSE}.}

\item{dest_dir}{Destination directory. If \code{NULL} (default) and \code{use_cache}
is \code{TRUE}, downloads to BIDS-compliant cache location:
\verb{\{cache\}/\{dataset_id\}/derivatives/\{pipeline\}/}.}

\item{use_cache}{If \code{TRUE} (default) and \code{dest_dir} is \code{NULL}, downloads to
CRAN-compliant cache location. Set \code{FALSE} to use current working directory.}

\item{quiet}{If \code{TRUE}, suppress all progress output. Default \code{FALSE}.}

\item{verbose}{If \code{TRUE}, show per-file progress in addition to overall
progress. Default \code{FALSE}.}

\item{force}{If \code{TRUE}, re-download files even if they exist with correct
size. Default \code{FALSE}.}

\item{backend}{Backend to use for downloading: "s3" or "https".
If \code{NULL} (default), auto-selects S3 for openneuro-derivatives bucket.}

\item{client}{An \code{openneuro_client} object. If \code{NULL}, creates default client.}
}
\value{
If \code{dry_run = TRUE}, returns a tibble with columns:
\describe{
\item{path}{Relative path within derivative}
\item{size}{File size in bytes}
\item{size_formatted}{Human-readable size (e.g., "1.2 GB")}
\item{dest_path}{Full destination path where file would be downloaded}
}

If \code{dry_run = FALSE}, invisibly returns a list with:
\describe{
\item{downloaded}{Number of files downloaded}
\item{skipped}{Number of files skipped (already cached)}
\item{failed}{Character vector of failed file names}
\item{total_bytes}{Total bytes downloaded}
\item{dest_dir}{Path to destination directory}
\item{backend}{Backend used for download}
}
}
\description{
Downloads fMRIPrep, MRIQC, or other derivative outputs from OpenNeuro
datasets. Supports filtering by subject, output space, and BIDS suffix.
Uses S3 backend (openneuro-derivatives bucket) with HTTPS fallback.
}
\details{
\subsection{Filter Logic}{

All filters combine with AND logic - a file must match ALL specified
filters to be included. For example, \verb{subjects = "sub-01", space = "MNI152NLin2009cAsym"}
downloads only sub-01's MNI-space files.
}

\subsection{Cache Structure}{

Derivatives are cached in BIDS-compliant structure:
\verb{\{cache_root\}/\{dataset_id\}/derivatives/\{pipeline\}/}

This keeps derivatives organized alongside raw data while maintaining
clear separation by pipeline.
}

\subsection{Backend Selection}{

S3 backend is preferred for the openneuro-derivatives bucket as it
provides fast parallel sync. HTTPS fallback is used if S3 is unavailable.
}

\subsection{Space Matching}{

Space matching is exact - specify the full space name (e.g.,
"MNI152NLin2009cAsym", not "MNI"). Files without a \verb{_space-} entity
(native/T1w space per BIDS convention) are always included when
filtering by space.
}
}
\examples{
\dontrun{
# Download all fMRIPrep derivatives for a dataset
on_download_derivatives("ds000001", "fmriprep")

# Download specific subjects
on_download_derivatives("ds000001", "fmriprep",
                        subjects = c("sub-01", "sub-02"))

# Download only MNI-space outputs
on_download_derivatives("ds000001", "fmriprep",
                        space = "MNI152NLin2009cAsym")

# Download only BOLD and mask files
on_download_derivatives("ds000001", "fmriprep",
                        suffix = c("bold", "mask"))

# Preview files without downloading
files <- on_download_derivatives("ds000001", "fmriprep",
                                  subjects = "sub-01",
                                  space = "MNI152NLin2009cAsym",
                                  dry_run = TRUE)
print(files)

# Combine all filters
on_download_derivatives("ds000001", "fmriprep",
                        subjects = regex("sub-0[1-5]"),
                        space = "MNI152NLin2009cAsym",
                        suffix = c("bold", "T1w"))
}

}
\seealso{
\code{\link[=on_derivatives]{on_derivatives()}} to discover available derivatives,
\code{\link[=on_spaces]{on_spaces()}} to discover available output spaces,
\code{\link[=on_download]{on_download()}} to download raw datasets
}
