% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphql.R
\name{on_request}
\alias{on_request}
\title{Execute GraphQL Query}
\usage{
on_request(query, variables = NULL, client = NULL)
}
\arguments{
\item{query}{A GraphQL query string.}

\item{variables}{A named list of variables to pass to the query.}

\item{client}{An \code{openneuro_client} object. If \code{NULL}, creates a default client.}
}
\value{
The \code{data} field from the GraphQL response.
}
\description{
Executes a GraphQL query against the OpenNeuro API. Handles authentication,
retry logic, rate limiting, and error handling.
}
\details{
The function implements several reliability features:
\itemize{
\item Automatic retry on transient errors (429, 500, 502, 503)
\item Rate limiting (10 requests per minute)
\item User-Agent header for API identification
\item Bearer token authentication when available
}

GraphQL errors (returned with HTTP 200 status) are detected and raised
as R errors with class \code{openneuro_api_error}.
}
\examples{
\dontrun{
# Execute a simple query
query <- "query { datasets(first: 1) { edges { node { id } } } }"
result <- on_request(query)
}

}
\seealso{
\code{\link[=on_client]{on_client()}} for creating client objects
}
