% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bids.R
\name{on_bids}
\alias{on_bids}
\title{Create BIDS Project from OpenNeuro Handle}
\usage{
on_bids(handle, fmriprep = FALSE, prep_dir = "derivatives/fmriprep")
}
\arguments{
\item{handle}{An \code{openneuro_handle} object, typically created with
\code{\link[=on_handle]{on_handle()}} and fetched with \code{\link[=on_fetch]{on_fetch()}}. If the handle is in
"pending" state, it will be automatically fetched first.}

\item{fmriprep}{Logical. If \code{TRUE}, include fMRIPrep derivatives from
the default \code{derivatives/fmriprep} path. Ignored if \code{prep_dir} is
specified. Default is \code{FALSE}.}

\item{prep_dir}{Character. Path to derivatives directory relative to
the dataset root. If specified, takes precedence over \code{fmriprep}.
Default is \code{"derivatives/fmriprep"}.}
}
\value{
A \code{bids_project} object from the bidser package.
}
\description{
Converts a fetched OpenNeuro dataset handle into a bidser \code{bids_project}
object, enabling BIDS-aware data access to subjects, sessions, files,
and derivatives.
}
\details{
This function provides a bridge between OpenNeuro's download system
and bidser's BIDS-aware data structures. The resulting \code{bids_project}
object exposes:
\itemize{
\item Subject and session information
\item BIDS file listings by modality
\item Derivatives access (if available)
}

The bidser package is required but listed as an optional dependency
(Suggests). If not installed, a helpful message guides installation.
}
\section{Derivatives Handling}{

When \code{fmriprep = TRUE}, the function looks for derivatives at
\code{derivatives/fmriprep} within the dataset. You can specify a custom
derivatives path with \code{prep_dir}.

If \code{prep_dir} is set to a non-default value, it takes precedence over
\code{fmriprep = TRUE}. A warning is issued if the requested derivatives
path does not exist.
}

\examples{
\donttest{
# Basic usage
handle <- on_handle("ds000001")
handle <- on_fetch(handle)
bids <- on_bids(handle)

# Auto-fetch if needed
handle <- on_handle("ds000002")
bids <- on_bids(handle)  # Fetches automatically

# Include fMRIPrep derivatives
bids <- on_bids(handle, fmriprep = TRUE)

# Custom derivatives path
bids <- on_bids(handle, prep_dir = "derivatives/custom-pipeline")
}
}
\seealso{
\code{\link[=on_handle]{on_handle()}} to create a handle, \code{\link[=on_fetch]{on_fetch()}} to download data.
}
