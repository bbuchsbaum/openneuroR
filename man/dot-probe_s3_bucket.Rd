% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-s3.R
\name{.probe_s3_bucket}
\alias{.probe_s3_bucket}
\title{Probe S3 Bucket Accessibility}
\usage{
.probe_s3_bucket(bucket, test_path = NULL, refresh = FALSE)
}
\arguments{
\item{bucket}{Character string: S3 bucket name (e.g., "openneuro.org").}

\item{test_path}{Character string: Optional path within bucket to test.
Useful for buckets with restricted ListObjectsV2 permissions.
If NULL, probes bucket root.}

\item{refresh}{Logical: If TRUE, bypass cache and probe again.}
}
\value{
Logical: TRUE if bucket is accessible, FALSE otherwise.
}
\description{
Tests whether an S3 bucket is accessible via anonymous access.
Results are cached per-session to avoid repeated network probes.
}
\details{
Uses \verb{aws s3 ls --no-sign-request} to test bucket access.
The probe has a 10-second timeout to avoid blocking on network issues.

Results are cached in \code{.discovery_cache} with key format
\verb{s3_bucket_probe_<bucket>} (or \verb{s3_bucket_probe_<bucket>_<test_path>}
if test_path is provided).
}
\examples{
\dontrun{
# Check if openneuro-derivatives is accessible
.probe_s3_bucket("openneuro-derivatives")

# Probe specific path (better for restricted buckets)
.probe_s3_bucket("openneuro-derivatives", test_path = "fmriprep/")

# Force refresh of cached result
.probe_s3_bucket("openneuro.org", refresh = TRUE)
}

}
\keyword{internal}
