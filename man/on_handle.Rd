% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handle.R
\name{on_handle}
\alias{on_handle}
\title{Create Lazy Handle to OpenNeuro Dataset}
\usage{
on_handle(dataset_id, tag = NULL, files = NULL, backend = NULL)
}
\arguments{
\item{dataset_id}{Dataset identifier (e.g., "ds000001").}

\item{tag}{Snapshot version tag. If NULL, uses latest snapshot when fetched.}

\item{files}{Character vector of specific files to download when fetched,
or a regex pattern. If NULL, downloads all files when fetched.}

\item{backend}{Backend to use when fetching: "datalad", "s3", or "https".
If NULL, auto-selects best available backend.}
}
\value{
An S3 object of class \code{openneuro_handle} with state "pending".
}
\description{
Creates a lazy handle that references an OpenNeuro dataset without
triggering an immediate download. The handle can be fetched later
when the data is actually needed.
}
\details{
Handles support a lazy evaluation pattern:
\enumerate{
\item Create handle with \code{on_handle()} - no download occurs
\item Fetch data with \code{on_fetch()} - download happens here
\item Get path with \code{on_path()} - returns filesystem path
}

This is useful for pipelines where dataset references need to be
defined early but data should only be downloaded when needed.
}
\section{Important}{

S3 objects have copy semantics. You must capture the return value
of \code{on_fetch()}:
\preformatted{
# WRONG - handle not updated
on_fetch(handle)
handle$state  # Still "pending"!

# CORRECT - capture returned handle
handle <- on_fetch(handle)
handle$state  # Now "ready"
}
}

\examples{
\donttest{
# Create lazy handle - no download yet
handle <- on_handle("ds000001", files = "participants.tsv")
print(handle)  # Shows state: pending

# Fetch when data is needed
handle <- on_fetch(handle)
print(handle)  # Shows state: ready

# Get filesystem path
path <- on_path(handle)
}
}
\seealso{
\code{\link[=on_fetch]{on_fetch()}} to materialize the download, \code{\link[=on_path]{on_path()}} to get path.
}
