% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery-spaces.R
\name{on_spaces}
\alias{on_spaces}
\title{Discover Available Output Spaces}
\usage{
on_spaces(derivative, refresh = FALSE, client = NULL)
}
\arguments{
\item{derivative}{A single-row tibble from \code{\link[=on_derivatives]{on_derivatives()}} output.
Must contain columns: \code{dataset_id}, \code{pipeline}, and \code{source}.}

\item{refresh}{If \code{TRUE}, bypass cache and fetch fresh data.
Default is \code{FALSE} to use cached results.}

\item{client}{An \code{openneuro_client} object for API calls (embedded sources).
If \code{NULL} (default), creates a default client.}
}
\value{
A character vector of space names, sorted alphabetically.
Common spaces include:
\itemize{
\item Volumetric: MNI152NLin2009cAsym, MNI152NLin6Asym, T1w
\item Surface: fsaverage, fsaverage5, fsaverage6, fsnative
}

Returns \code{character(0)} with a warning if no spaces are found.
}
\description{
Discovers the available output spaces (MNI152NLin2009cAsym, fsaverage, etc.)
for a derivative dataset. Parses BIDS \verb{_space-} entity from filenames.
}
\details{
\subsection{Space Discovery}{

This function samples derivative files and extracts the \verb{_space-<label>}
entity from BIDS-formatted filenames. It does NOT infer T1w from files
without a space entity (per BIDS convention, native space files may
omit the space entity).
}

\subsection{Source Handling}{
\itemize{
\item \strong{embedded}: Uses the OpenNeuro API to list files in the
\verb{derivatives/\{pipeline\}/} directory.
\item \strong{openneuro-derivatives}: Uses AWS CLI to list files from the
\verb{s3://openneuro-derivatives/} bucket.
}
}

\subsection{Caching}{

Results are cached per-session to minimize API/S3 calls. Use
\code{refresh = TRUE} to bypass the cache.
}
}
\examples{
\dontrun{
# First, get available derivatives for a dataset
derivs <- on_derivatives("ds000102")
print(derivs)

# Then get spaces for the first derivative
spaces <- on_spaces(derivs[1, ])
print(spaces)
# Example output: c("MNI152NLin2009cAsym", "fsaverage")

# Force refresh of cached spaces
spaces <- on_spaces(derivs[1, ], refresh = TRUE)
}

}
\seealso{
\code{\link[=on_derivatives]{on_derivatives()}} to discover available derivative datasets
}
